# MVP Project Status

기준일: 2026-07-11

## 한 줄 요약

현재 MVP는 보호구역 데이터 추출, 취합, 변경 감지 구조, 알림까지 완료에 가깝고, 남은 핵심은 오류 안정화와 표준노드링크 후보 매칭 검증이다.

## 현재 구현 상태와 목표 상태

| 영역 | MVP 목표 | 현재 상태 | 판정 | 다음 작업 |
|---|---|---|---|---|
| 공공데이터 수집 | 보호구역 API에서 지정 지역 데이터를 가져온다 | 데이터 추출 가능 | 완료 | 오류 케이스별 재시도와 실패 기록 보강 |
| 원본 저장 | API 원본 응답을 실행 회차별 스냅샷으로 저장한다 | 원본 취합 구조 있음 | 완료 | 실행 회차별 원본 보존 여부 점검 |
| 데이터 정규화 | 보호구역 속성, 좌표, geometry를 분석용 구조로 변환한다 | 정규화 파이프라인 구성됨 | 완료에 가까움 | 일부 오류 데이터 처리 기준 정리 |
| 데이터 취합 | 여러 응답과 지역 데이터를 하나의 분석 구조로 모은다 | 취합 가능 | 완료 | 중복, 누락, 빈 응답 처리 검증 |
| 변경 감지 | 신규, 속성 변경, geometry 변경, 삭제를 구분한다 | 구조와 로직 있음 | 부분 완료 | 반복 실행과 샘플 변경 테스트로 검증 |
| 변경 이력 저장 | 변경 이벤트를 DB에 누적 저장한다 | 이력 테이블 구조 있음 | 부분 완료 | 이벤트 타입별 검증 리포트 작성 |
| 실행 로그 | 실행 성공, 실패, 건수, 메시지를 남긴다 | 기본 구조 있음 | 부분 완료 | 사람이 바로 이해할 수 있는 오류 메시지 보강 |
| 알림 | 변경 발생 또는 실패 시 알림을 보낸다 | 알림 구현됨 | 완료에 가까움 | 변경 없음, 실패, 부분 실패 알림 정책 분리 |
| 자동 실행 | 매일 09:00 KST에 실행한다 | 문서와 실행 방향 있음 | 부분 완료 | 실제 스케줄러 또는 self-hosted runner 안정화 |
| 표준노드링크 데이터 | PostGIS에 표준노드링크를 적재해 보호구역과 비교한다 | 로컬 DB 적재 완료 | 완료 | 운영 전 DB 의존성 문서화 |
| 후보 매칭 | 보호구역과 도로 후보를 생성한다 | 표준노드링크 후보 매칭 검증 중이며, NGII 도로중심선 서울/경기 보조 기준선 검증을 추가 | 진행 중 | 표준노드링크와 NGII 도로중심선 후보 품질 비교 |
| 매칭 검수 | 후보 결과를 사람이 확인할 수 있다 | 검토 문서와 QGIS 확인 흐름 있음 | 부분 완료 | 검수용 테이블, 뷰, 스타일 고정 |
| 확정 매칭 | 검수된 후보를 확정 관계로 저장한다 | MVP 이후 범위 | 미완료 | 후보 매칭 검증 이후 별도 단계로 진행 |
| 웹 지도 | 보호구역, 변경 이력, 매칭 상태를 지도에서 본다 | 계획 단계 | 미완료 | MVP 이후 간단한 내부 대시보드부터 진행 |
| 통계 화면 | 변경 건수, 지역별 현황, 매칭 상태를 본다 | 계획 단계 | 미완료 | DB 뷰 또는 정적 리포트 우선 |
| 운영 배포 | 저비용으로 장기 운영한다 | 로컬 중심 구상 | 부분 완료 | 로컬 운영 유지, 클라우드 이전은 후순위 검토 |
| README/포트폴리오 | 프로젝트 목적과 현재 상태를 설명한다 | 초안 있음 | 부분 완료 | MVP 기준 README 재정리 |

## MVP 완료 기준

MVP는 다음 조건이 충족되면 완료로 판단한다.

| 완료 기준 | 확인 방법 | 현재 판정 |
|---|---|---|
| 지정 지역 데이터 수집이 반복 실행된다 | 수동 실행 또는 자동 실행 로그 확인 | 완료에 가까움 |
| 같은 데이터를 다시 실행하면 불필요한 변경 이벤트가 생기지 않는다 | idempotency 검증 | 검증 필요 |
| 신규, 변경, 삭제 샘플이 의도대로 감지된다 | 샘플 변경 테스트 | 검증 필요 |
| 실패 시 원인을 추적할 수 있다 | `ops.pipeline_run`, 로그, 알림 확인 | 보강 필요 |
| 변경이 있을 때 알림이 온다 | Telegram 또는 Slack 알림 확인 | 완료에 가까움 |
| 변경 없음과 실패가 구분되어 기록된다 | 실행 로그와 알림 문구 확인 | 보강 필요 |
| 표준노드링크 후보 매칭 결과를 검수할 수 있다 | QGIS 뷰, 검수 문서, 후보 집계 확인 | 진행 중 |

## MVP 이후 범위

다음 항목은 MVP 완료 이후 확장 단계에서 다룬다.

- 전국 전체 자동 운영
- 보호구역-표준링크 자동 확정 매칭
- 웹 지도와 통계 대시보드
- 클라우드 DB 또는 외부 서버 배포
- 사용자 계정, 권한, 공개 서비스 운영
- 포트폴리오용 시각 자료와 사례 페이지

## 업데이트 규칙

이 문서는 프로젝트의 중앙 상태판으로 사용한다. 각 작업 대화방에서 진전이 생기면 해당 대화의 마지막에 이 문서 업데이트 여부를 확인한다.

권장 업데이트 시점:

| 상황 | 업데이트 여부 | 예시 |
|---|---|---|
| 기능이 새로 완료됨 | 필수 | 알림 발송 성공, 자동 실행 안정화 |
| 검증 결과가 나옴 | 필수 | 반복 실행 PASS, 서울 전체 실행 PASS |
| MVP 범위가 바뀜 | 필수 | 웹 지도를 MVP 이후로 이동 |
| 오류 유형이 새로 정리됨 | 권장 | API 빈 응답, geometry 오류 처리 기준 |
| 문서만 소폭 수정됨 | 선택 | 표현 수정, 링크 수정 |
| 실험 중인 아이디어 | 보류 | 아직 채택하지 않은 매칭 기준 |

각 채팅방에서 Codex에게 줄 수 있는 문장:

```text
이번 작업 결과를 기준으로 docs/mvp_project_status.md의 완료/미완료 표와 다음 작업을 업데이트해줘.
현재 구현 상태와 목표 상태를 구분하고, 변경된 결정사항이 있으면 이유와 다음 작업도 적어줘.
```

## 최근 결정사항

| 날짜 | 결정사항 | 이유 | 다음 작업 |
|---|---|---|---|
| 2026-07-11 | 현재 MVP 상태는 데이터 추출, 취합, 알림까지 완료에 가깝고 후보 매칭 검증은 진행 중으로 본다 | 핵심 파이프라인은 구성되었지만 오류 안정화와 검증 리포트가 남아 있음 | 오류 유형표와 반복 실행 검증 결과 문서화 |
| 2026-07-11 | 웹 지도와 통계 화면은 MVP 필수 완료 조건에서 제외한다 | 데이터 파이프라인 신뢰성이 먼저이며 웹까지 포함하면 범위가 커짐 | 웹은 MVP 이후 1차 확장으로 분리 |
| 2026-07-11 | 표준노드링크 매칭은 자동 확정이 아니라 후보 생성과 검증을 현재 목표로 둔다 | 오탐 가능성이 있어 초기부터 자동 확정하면 리스크가 큼 | 후보 기준, 검수 상태값, 확정 흐름 정리 |
| 2026-07-11 | 이 문서를 프로젝트 중앙 상태판으로 사용한다 | 여러 대화방에서 작업이 나뉘어도 현재 상태를 한곳에서 확인하기 위함 | 주요 작업 후 이 문서를 업데이트하도록 Codex에 지시 |
| 2026-07-20 | NGII 도로중심선은 서울/경기 범위만 우선 사용하고, 도로명주소 도로구간과 실폭도로는 이번 단계에서 제외한다 | 전국 병합/검증 비용이 크고, 도로명주소 도로구간은 경로탐색 네트워크로 부적합하며 실폭도로는 면형 데이터임 | 5번 NGII 도로중심선 PostGIS 방에서 원본 보존, 단순화본 생성, QC 기준을 관리 |

## 다음 Codex 작업 단위

| 우선순위 | 작업 | 산출물 |
|---|---|---|
| 1 | 오류 유형 정리 | 오류 유형표, 실패 대응 기준 |
| 2 | 반복 실행 검증 | idempotency 검증 결과 |
| 3 | 변경 샘플 테스트 | 신규, 속성 변경, geometry 변경, 삭제 검증 결과 |
| 4 | 알림 정책 정리 | 변경 있음, 변경 없음, 실패, 부분 실패 메시지 기준 |
| 5 | 후보 매칭 검증표 작성 | 후보 등급, 검수 상태, 수동 확인 기준 |
| 6 | NGII 도로중심선 서울/경기 단순화 QC | 0.3m, 0.5m, 1.0m 후보별 형상 훼손, 연결성, 보호구역 주변 후보 변화 비교 |
| 7 | README 재정리 | 현재 구현 상태와 MVP 이후 범위가 구분된 README |
